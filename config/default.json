{
  "database": {
    "host": "${PGHOST:-localhost}",
    "port": "${PGPORT:-5432}",
    "database": "${PGDATABASE:-evershop}",
    "user": "${PGUSER:-postgres}",
    "password": "${PGPASSWORD:-postgres}",
    "ssl": {
      "rejectUnauthorized": false,
      "sslmode": "require"
    }
  }
}
